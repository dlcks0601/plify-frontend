import type { Metadata } from 'next';
import '../ui/globals.css';
import Providers from './components/Providers/Providers';
import ThemeProvider from './components/Providers/ThemeProvider';
import NavWrapper from './components/Common/layouts/NavWrapper';
import PlayerWrapperContainer from './components/Common/layouts/PlayerWrapperContainer';

export const metadata: Metadata = {
  title: 'Plify',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang='en' suppressHydrationWarning>
      <body className='flex flex-col min-h-screen bg-white dark:bg-black text-black dark:text-white'>
        <ThemeProvider>
          <Providers>
            <NavWrapper />
            <main className='flex-grow bg-white dark:bg-black text-black dark:text-white'>
              {children}
            </main>
            <PlayerWrapperContainer />
          </Providers>
        </ThemeProvider>
      </body>
    </html>
  );
}
